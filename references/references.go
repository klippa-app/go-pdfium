package references

// FPDF_DOCUMENT is just an alias for string, but it's useful to keep the alias,
// so that we can change it later. Should contain a UUID.
type FPDF_DOCUMENT string

// FPDF_PAGE is just an alias for string, but it's useful to keep the alias,
// so that we can change it later. Should contain a UUID.
type FPDF_PAGE string
