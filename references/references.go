package references

// Document is just an alias for string, but it's useful to keep the alias,
// so that we can change it later. Should contain a UUID.
type Document string

// Page is just an alias for string, but it's useful to keep the alias,
// so that we can change it later. Should contain a UUID.
type Page string
